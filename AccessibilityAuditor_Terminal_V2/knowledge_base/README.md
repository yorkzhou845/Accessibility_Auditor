# Knowledge base

Place generic `.md` or `.txt` reference material in this directory. On the next run, the application creates or refreshes `data/vector_store.csv` by requesting embeddings from the configured local Ollama model.

Do not add confidential workplace documents, licensed standards text that you cannot redistribute, personal information, or production data.
